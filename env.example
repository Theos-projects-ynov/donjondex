# Configuration pour déploiement NAS
# Copiez ce fichier vers .env et adaptez les valeurs selon votre NAS

# === BASE DE DONNÉES ===
POSTGRES_USER=pokemaster
POSTGRES_PASSWORD=pokepass
POSTGRES_DB=clickerpokemon
DATABASE_URL=postgresql://pokemaster:pokepass@postgres:5432/clickerpokemon

# === PGADMIN ===
PGADMIN_DEFAULT_EMAIL=admin@poke.com
PGADMIN_DEFAULT_PASSWORD=admin123

# === BACKEND ===
NODE_ENV=production
PORT=4000
WS_PORT=4001

# === FRONTEND ===
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4001

# === PORTS (pour NAS) ===
# Adaptez selon votre configuration réseau
FRONTEND_PORT=3000
BACKEND_PORT=4000
WS_PORT=4001
POSTGRES_PORT=5432
PGADMIN_PORT=5050

# === VOLUMES (pour NAS) ===
# Chemins sur votre NAS (exemple Synology)
POSTGRES_DATA_PATH=/volume1/docker/pokemon/postgres
APP_DATA_PATH=/volume1/docker/pokemon/app

# === SÉCURITÉ ===
# ⚠️ CHANGEZ CES VALEURS EN PRODUCTION ! ⚠️
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
BCRYPT_ROUNDS=12

# === CONFIGURATION NAS ===
# Pour Synology/QNAP, adaptez selon votre setup
# Exemple pour accès externe :
# VITE_API_URL=http://192.168.1.100:4000
# VITE_WS_URL=ws://192.168.1.100:4001
